
typedef struct server_ftp_s {
	int i;
} server_ftp_t;

typedef struct client_ftp_s {
	int i;
} client_ftp_t;


int main()
{
	// init server ftp
	server_ftp * server_ftp;
	server_ftp = malloc(sizeof(server_ftp));

	// init client ftp
	client_ftp * client_ftp;
	clienf_ftp = malloc(sizeof(client_ftp));


	return 0;
}
